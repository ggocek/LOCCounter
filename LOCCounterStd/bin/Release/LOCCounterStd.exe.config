<?xml version="1.0" encoding="utf-8" ?>
<configuration>
	<configSections>
		<section name="LOCCounterExtensionsSection" type="Sloc.LOCCConfigurationSection, Sloc" requirePermission="false" />
	</configSections>
	<!-- 
		At run time, a 'name' (extension) below is not case sensitive. An
		extension matches the end of a file name after the last period. The
		Sloc code library contains a default extension that looks like the 'c'
		extension as originally provided, i.e., if a file's extension is not
		found in this file, it uses the hardcoded default, and there is no way
		to adjust that here.
		The 'value' is a comma separated list of comment delimiters. Each
		extension can contain any number of delimiters. If a delimiter is a
		single string, it represents the comment delimiter for a single-line
		comment. If a delimiter is two strings separated by a space, the first
		string is the starting delimiter for a multi-line comment, and the
		second string is the ending delimiter for a multi-line comment.
		Certain special characters like angle brackets must be escaped,
		see 'aspx' for an example.
		Note that when the applications are executed, the user selects the
		extensions to process. This config file describes the comment
		delimiters, not normally modified by end users. As originally
		downloaded, this file describes many common code-file extensions.
		Change this config file if you need to describe delimiters for new or
		custom extensions.
	-->
	<LOCCounterExtensionsSection>
		<extensions>
			<add name="c" value="//, /* */" />
			<add name="h" value="//, /* */" />
			<add name="cs" value="//, /* */" />
			<add name="java" value="//, /* */" />
			<add name="js" value="//, /* */" />
			<add name="sql" value="--, /* */" />
			<add name="asp" value="', rem, REM" />
			<add name="vb" value="', rem, REM" />
			<add name="vbs" value="', rem, REM" />
			<add name="bat" value="', rem, REM" />
			<add name="cmd" value="', rem, REM" />
			<add name="aspx" value="//, /* */, &lt;!-- --&gt;" />
			<add name="ascx" value="//, /* */, &lt;!-- --&gt;" />
			<add name="ashx" value="//, /* */, &lt;!-- --&gt;" />
			<add name="asmx" value="//, /* */, &lt;!-- --&gt;" />
			<add name="asax" value="//, /* */, &lt;!-- --&gt;" />
			<add name="htm" value="//, /* */, &lt;!-- --&gt;" />
			<add name="html" value="//, /* */, &lt;!-- --&gt;" />
			<add name="xml" value="&lt;!-- --&gt;" />
			<add name="xsl" value="&lt;!-- --&gt;" />
			<add name="resx" value="&lt;!-- --&gt;" />
			<add name="config" value="&lt;!-- --&gt;" />
			<add name="ism" value="&lt;!-- --&gt;" />
			<add name="php" value="//, /* */" />
			<add name="py" value="#" />
			<add name="proj" value="&lt;!-- --&gt;" />
			<add name="targets" value="&lt;!-- --&gt;" />
			<add name="master" value="//, /* */, &lt;!-- --&gt;" />
		</extensions>
	</LOCCounterExtensionsSection>
</configuration>
